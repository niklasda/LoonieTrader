<Window x:Class="Oanda.App.Windows.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:local="clr-namespace:Oanda.App.Windows"
        xmlns:dgx="urn:tom-englert.de/DataGridExtensions"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
        mc:Ignorable="d"
        Title="TestWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1"  Margin="0" dgx:DataGridFilter.IsAutoFilterEnabled="True" />

        <oxy:PlotView Grid.Row="0"  Model="{Binding GraphData}" BorderBrush="Black" BorderThickness="1" />
    </Grid>
</Window>
