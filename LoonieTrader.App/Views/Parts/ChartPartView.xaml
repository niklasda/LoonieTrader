<UserControl x:Class="LoonieTrader.App.Views.Parts.ChartPartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LoonieTrader.App.Views.Parts"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:windows="clr-namespace:LoonieTrader.App.ViewModels.Windows"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance d:Type=windows:ChartWindowViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Content="Update" Margin="30,5" Command="{Binding UpdateCommand}"/>
        <wpf:CartesianChart Grid.Row="1" Series="{Binding SeriesCollection}" BorderThickness="1" BorderBrush="Gainsboro">
            <wpf:CartesianChart.AxisX>
                <wpf:Axis Labels="{Binding Labels}"/>
            </wpf:CartesianChart.AxisX>
        </wpf:CartesianChart>

    </Grid>
</UserControl>
